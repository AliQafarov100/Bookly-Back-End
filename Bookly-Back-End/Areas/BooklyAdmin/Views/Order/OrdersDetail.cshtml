@*@model OrderVM
@{
    ViewData["Title"] = "OrdersDetail";
}

<div>
    <div class="d-flex">
        <p>Id: </p>
        <h4 class="ms-3">@Model.Order.Id</h4>
    </div>
    <div class="d-flex">
        <p>Delivery Address:</p>
        <h4 class="ms-3">@Model.Order.Address</h4>
    </div>
    <div class="d-flex">
        <p>Client Fullname:</p>
        <h4 class="ms-3">@(Model.Order.AppUser.FirstName + " " + Model.Order.AppUser.LastName)</h4>
    </div>
    <div class="d-flex">
        <p>Client Email:</p>
        <h4 class="ms-3">@Model.Order.AppUser.Email</h4>
    </div>
    <div class="d-flex">
        <p>Delivery City:</p>
        <h4 class="ms-3">@Model.Order.City.Name</h4>
    </div>
    <div class="d-flex">
        <p>Order Date:</p>
        <h4 class="ms-3">@Model.Order.OrderDate</h4>
    </div>
    <div class="d-flex">
        <p>Order TotalPrice:</p>
        <h4 class="ms-3">@Model.Order.TotalPrice</h4>
    </div>
    <h4>Order Product Item:</h4>
    <div class="row">
        @foreach (BasketItem item in Model.BasketItems)
        {
            <div class="col-lg-4 mt-2">
                <img class="" style="height: 650px; width: 90%;" src="~/assets/Image/Library/@item.Book.BookImages.FirstOrDefault(i => i.IsMain == true)?.ImagePath" /> <br />
                <strong>@item.Book.Name</strong> <br />
                <strong>
                    $
                    @if (item.Book.DiscountId != null)
                    {
                        @(item.Book.Price - ((item.Book.Price * item.Book.Discount.DiscountPercent) / 100))
                    }
                    else
                    {
                        @item.Book.Price
                    }
                </strong> <br />
                <strong>Count: @item.Count</strong>
            </div>
        }
    </div>
</div>*@

